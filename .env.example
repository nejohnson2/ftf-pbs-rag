# Copy this file to .env and fill in your values.
# .env is gitignored — never commit secrets.

# Heroku Postgres connection string (set automatically on Heroku)
DATABASE_URL=postgresql://user:password@localhost:5432/ftf_rag

# Ollama remote server
OLLAMA_BASE_URL=http://your-ollama-server:11434
OLLAMA_MODEL=llama3.2
OLLAMA_EMBED_MODEL=nomic-embed-text

# Secret key for signing session cookies (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=change_me_to_a_random_secret

# Optional: override embedding provider
# EMBEDDING_PROVIDER=ollama
